# Results Overview

This directory captures evidence for AccentNet experiments.

- `audio_samples/` – paired waveforms (source, converted, ground-truth). Filenames follow the pattern `<utterance>_original.wav` for raw audio and `<utterance>_ukconverted.wav` for outputs synthesized by our accent conversion model.
